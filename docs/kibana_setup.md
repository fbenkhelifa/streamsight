# STREAMSIGHT — Kibana Setup

## Automatic setup

Run:

- `python setup_kibana.py`

This creates:

- data view: `streamsight-trades`
- dashboard: `streamsight-dashboard`

## Manual fallback

Import `kibana_dashboard.ndjson` in Kibana Saved Objects.
